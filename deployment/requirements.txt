# Neko Speech Deployment Dependencies
# ================================
# Install: pip install -r requirements.txt

# Core
torch>=2.0.0
numpy>=1.24.0

# ONNX Export & Runtime
onnx>=1.14.0
onnxruntime>=1.16.0        # CPU inference
# onnxruntime-gpu>=1.16.0  # GPU inference (optional, CUDA)
onnxslim>=0.1.0            # ONNX graph optimization (optional)

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Benchmarking
psutil>=5.9.0
tabulate>=0.9.0
matplotlib>=3.7.0

# MNN (optional, for mobile deployment)
# Install MNN via: pip install MNN (if available for your platform)
# Or build from source: https://github.com/alibaba/MNN

# sherpa-onnx (optional, for desktop/mobile TTS)
# pip install sherpa-onnx
